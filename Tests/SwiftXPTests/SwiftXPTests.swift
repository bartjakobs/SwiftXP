import XCTest
@testable import SwiftXP

final class SwiftXPTests: XCTestCase {
    func testExample() throws {
        // no tests, because SwiftUI is testable by looking at the UI,
        // according to Apple.
        // IDK.
        
    }
}
